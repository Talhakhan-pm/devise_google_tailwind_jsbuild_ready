<div class= "flex px-2 justify-center w-full h-screen items-center">
<% if current_user  %>
  <p class="text-2xl py-4 px-4"><%= current_user.email%></p>
  <%= current_user.name %>
  <div class="my-8">
  <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-red-400 border-red-600 border-2 py-2 px-4 rounded-lg mt-2" %>
  <%= link_to "Edit User", edit_user_registration_path, class: "text-yello-400 border-yellow-600 border-2 py-2 px-4 rounded-lg mt-2 bg-yellow-200 text-gray-500 " %>
  </div>
<% end %>
</div>
